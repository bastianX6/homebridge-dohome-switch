{
  "pluginAlias": "DoHomeSwitch",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Switch 1"
      },
      "prodname": {
        "title": "Usually Last 4 Character of Mac Address",
        "type": "string",
        "required": true,
        "default": "65ff"
      },
      "host": {
        "title": "Device IP/host (unicast)",
        "type": "string",
        "description": "Direct device address. Prefer this for Docker or segmented networks where broadcast fails.",
        "required": false
      },
      "subnet": {
        "title": "UDP destination (usually broadcast)",
        "type": "string",
        "description": "Broadcast address for legacy behavior (e.g. 192.168.0.255). Used when host is not provided.",
        "default": "192.168.0.255"
      },
      "port": {
        "title": "UDP podcast path",
        "type": "integer",
        "default": 6091
      },
      "deviceid": {
        "title": "Device ID",
        "type": "string",
        "default": "955sga0g65ff_DT-PLUG_HOMEKIT"
      }
    }
  }
}